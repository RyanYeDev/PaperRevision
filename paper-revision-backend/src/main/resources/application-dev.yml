spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/paper_revision
    username: postgres
    password: postgres

logging:
  level:
    com.paperrevision: DEBUG
    org.springframework.web: INFO
